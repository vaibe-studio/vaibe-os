"""CLI utility for generating images via ComfyUI API.

Usage:
    python -m tools.image_gen generate --character "Алекс Байтов" --scene "pointing at holographic screen"
    python -m tools.image_gen generate --prompt "custom prompt" --output ./output.png
    python -m tools.image_gen status
    python -m tools.image_gen list-characters
"""
